/**
 * index.js — Legacy stub (not used by WXT build)
 *
 * The actual extension entry points are TypeScript files managed by WXT:
 *
 *   entrypoints/background.ts  — Service worker (context menu, side panel)
 *   entrypoints/content.ts     — Content script (canvas overlay, logging)
 *   entrypoints/sidebar/       — React sidebar panel (list, download, toggle)
 *
 * To develop:  npm run dev
 * To build:    npm run build
 * To package:  npm run zip
 */